# Edited by N4zz

conky.config= {

    --Various settings

    update_interval = 1,
    background = false,                    -- forked to background
    cpu_avg_samples = 2,                   -- The number of samples to average for CPU monitoring.
    net_avg_samples = 2,                   -- The number of samples to average for net data
    no_buffers = true,                     -- Subtract (file system) buffers from used memory?
    double_buffer = true,                  -- Use the Xdbe extension? (eliminates flicker)
    update_interval = 1.0,
    temperature_unit = 'celsius',          -- fahrenheit or celsius
    text_buffer_size = 2048,               -- size of buffer for display of content of large variables - default 256
    diskio_avg_samples = 10,               -- The number of samples to average for disk I/O monitoring


    --Placement

    alignment = 'bottom_left',               -- top_left,top_middle,top_right,bottom_left,bottom_middle,bottom_right,
    gap_x = 50,                            -- own window move right or left
    gap_y = 340,                           -- own window move up or down
    minimum_height = 180,
    minimum_width = 300,
    maximum_width = 350,


    --Graphical

    draw_borders = false,
    draw_graph_borders = false,
    draw_outline = false,
    draw_shades = true,
    border_width = 1,
    default_bar_height = 5,
    default_bar_width = 340,


    --Textual

    extra_newline = false,
    format_human_readable = true,          -- KiB, MiB rather then number of bytes
    --font = 'sans:size=10',
    --font = 'xftfont 123:size=10',
    --font = 'xftfont Roboto Condensed:size=9',
    font = 'Orbitron:bold:size=8',
    max_text_width = 0,                    -- 0 will make sure line does not get broken if width too smal
    max_user_text = 16384,                 -- max text in conky default 16384
    override_utf8_locale = true,           -- force UTF8 requires xft
    short_units = true,                    -- shorten units from KiB to k
    top_name_width = 21,                   -- width for $top name value default 15
    top_name_verbose = false,              -- If true, top name shows the full command line of  each  process - Default value is false.
    uppercase = false,
    use_spacer = 'none',                   -- adds spaces around certain objects to align - default none
    use_xft = true,                        -- xft font - anti-aliased font
    xftalpha = 1,                          -- alpha of the xft font - between 0-1

    --Window

    own_window = true,
    own_window_title = 'network_monitor',
    own_window_class = 'Conky',
    own_window_type = 'normal',            -- if own_window true options are: normal/override/dock/desktop/panel
    own_window_argb_visual = true,
    own_window_argb_value = 125,
    own_window_transparent = true,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager', -- undecorated,


    --Colours

    default_color = 'grey',
    default_outline_color = 'white',
    default_shade_color = 'black',
    default_border_color = 'none',

    color1 = '00d4ff',    -- jasně modrá (hlavní akcenty)
    color2 = '0099cc',    -- střední modrá (sekundární text)
    color3 = '66ffff',    -- světle cyan (hodnoty/čísla)
    color4 = '004466',    -- tmavě modrá (méně důležité info)
    color5 = '66ffff',
    --color6 =,
    color7 = '888888',    -- šedá (labels)
    color8 = '004477',    -- tmavě modrá (grafy)
    --color8 = '1abc9c',    -- manjaro light green

};

conky.text = [[
${voffset 5}${alignc}${color1}${font Neuropol X:bold:size=11}HARDWARE
${image ~/.config/conky/universe/spacer.png -d -p 55,27}${image ~/.config/conky/universe/spacer.png -d -p 55,27}
#----------------------------- C P U ------------------------------------#
${voffset -10}${goto 5} ${font}${color1} CPU : ${color3} ${cpu cpu} % ${goto 105} ${color2} FREQ : ${color3} ${freq_g} GHz ${goto 220} ${color2} TMP : ${color3} ${hwmon 2 temp 1}°C
${voffset 5}${goto 5}${color5}${cpugraph cpu0 35, 280 004477 0099cc -t}
#----------------------------- G P U  NVidia  -------------------------------------#
${voffset 5}${goto 5} ${color1} GPU : ${color3}${execi 5 nvidia-smi --query-gpu=clocks.gr --format=csv,noheader,nounits} Mhz ${goto 105} ${color2} MEM : ${color3} ${execi 5 nvidia-smi --query-gpu=clocks.mem --format=csv,noheader,nounits} Mhz ${goto 220} ${color2} TMP : ${color3} ${execi 5 nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader,nounits}°C
#----------------------------- R A M --------------------------------------#
${voffset 15}${goto 5} ${color1} RAM : ${color3} $memperc% ${goto 105} ${color2} USE  : ${color3} $mem ${goto 220} ${color2} TOT : ${color3} $memmax
]];
